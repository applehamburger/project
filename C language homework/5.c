struct in()
{
  
  char name[50];
  char groupname;
  int w;
  int l;
  int e;	
}
#include <stdio.h>
int main()
{
	int m,i,s;
	scanf("%d",&m);
	for (i=0;i<m;i++)
	{
	struct in;
	scanf("%s",in.name);
	scanf("%d",in.w);
	scanf("%d",in.l);
	scanf("%d",in.e);
    }
    s=w*3+e;
    for(i=0;i<m;i++)
    {
	printf (struct in[i]);
	return 0;
}
